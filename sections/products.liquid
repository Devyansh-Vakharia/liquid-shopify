{% comment %}
The following is a Liquid code template for a Shopify theme. It integrates the provided HTML structure with Liquid tags for dynamic content and functionality.
{% endcomment %}

<!DOCTYPE html>
<html lang="{{ shop.locale }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ page_title }}</title>
    {{ content_for_header }}
    <style>
        body {
            font-family: 'Helvetica Neue', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f8f8f8;
            color: #333;
        }

        /* Add your custom CSS styles here */

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        .product-details {
            background-color: #fff;
            padding: 30px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
        }

        .product-description h1 {
            font-size: 24px;
            margin-bottom: 10px;
        }

        .product-description p {
            font-size: 16px;
            color: #555;
            margin-top: 0;
        }

        .product-features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .feature-item {
            background-color: #f0f0f0;
            border-radius: 8px;
            padding: 15px;
            text-align: center;
            box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
        }

        .feature-item img {
            max-width: 100%;
            border-radius: 5px;
            margin-bottom: 10px;
        }

        .feature-item p {
            font-size: 14px;
            color: #444;
            margin: 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="product-details">
            <div class="product-description">
                <h1>{{ product.title }}</h1>
                <p>{{ product.description }}</p>
            </div>
            <div class="product-features">
                {% for image in product.images %}
                <div class="feature-item">
                    <img src="{{ image.src | img_url: '500x' }}" alt="{{ image.alt | escape }}">
                    <p>{{ image.alt }}</p>
                </div>
                {% endfor %}
            </div>
        </div>
    </div>
</body>
</html>
{% schema %}
{
  "name": "collection-with-text",
  "presets": [
    {
      "name": "collection-with-text",
      "category": "Collection"
    }
  ]
}
{% endschema %}
  