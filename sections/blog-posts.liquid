{% comment %}
  Schema Definition:
  - Allow the merchant to select a collection to feature
  - Optionally allow them to add a title and description
{% endcomment %}

{% schema %}
{
  "name": "Featured Collection",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Featured Collection"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Explore our handpicked selection"
    },
    {
      "type": "collection",
      "id": "featured_collection",
      "label": "Featured Collection"
    }
  ],
  "presets": [
    {
      "name": "Featured Collection",
      "category": "Products"
    }
  ]
}
{% endschema %}

{% comment %}
  CSS Styling:
  - Style the featured collection section to match the design of frido.com
{% endcomment %}

<style>
  .featured-collection {
    text-align: center;
    padding: 50px 0;
  }

  .featured-collection-title {
    font-size: 24px;
    font-weight: bold;
    margin-bottom: 20px;
  }

  .featured-collection-description {
    font-size: 16px;
    color: #666;
    margin-bottom: 30px;
  }

  .featured-products {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 20px;
    max-width: 1200px;
    margin: 0 auto;
  }

  .featured-product {
    border: 1px solid #ddd;
    border-radius: 5px;
    overflow: hidden;
    transition: transform 0.3s ease;
  }

  .featured-product:hover {
    transform: translateY(-5px);
  }

  .featured-product-image {
    width: 100%;
    height: 200px;
    object-fit: cover;
  }

  .featured-product-title {
    font-size: 18px;
    font-weight: bold;
    margin: 10px 0;
  }

  .featured-product-price {
    font-size: 16px;
    color: #666;
  }
</style>

{% comment %}
  HTML Structure:
  - Display the title, description, and featured products
{% endcomment %}

<div class="featured-collection">
  <div class="featured-collection-title">{{ section.settings.title }}</div>
  <div class="featured-collection-description">{{ section.settings.description }}</div>
  
  <div class="featured-products">
    {% for product in collections[section.settings.featured_collection].products limit: 4 %}
      <div class="featured-product">
        <a href="{{ product.url }}">
          <img class="featured-product-image" src="{{ product.featured_image | img_url: 'medium' }}" alt="{{ product.title }}">
          <div class="featured-product-title">{{ product.title }}</div>
          <div class="featured-product-price">{{ product.price | money }}</div>
        </a>
      </div>
    {% endfor %}
  </div>
</div>
